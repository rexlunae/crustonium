# Workspace Structure Test
#
# Phase 1.1: Research and Prototyping
# This crate tests the Cargo workspace structure for a monorepo

[package]
name = "workspace-structure-test"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
# Test workspace dependency resolution
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
# Test that dev dependencies work correctly
criterion = { workspace = true }
