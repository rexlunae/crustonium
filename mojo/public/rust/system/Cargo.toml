[package]
name = "mojo-rust-system-api"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

# This is a Tier 2 component requiring GN-generated bindings
# See README.md for build instructions

[lib]
name = "mojo_rust_system_api"
path = "lib.rs"

[dependencies]
bitflags = "2.0"

[build-dependencies]
# Currently relies on GN-generated bindings via chromium::import!
# Future: May need build.rs to check for GN output

# Note: This component uses chromium::import! macro to import
# GN-generated Rust bindings for the Mojo C API. The bindings
# are generated by GN and must be available before Cargo build.
